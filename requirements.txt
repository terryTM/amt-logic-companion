# Core pipeline: Logic project parsing + Anticipatory Music Transformer.
# Versions are the ones the demo was tested with (Python 3.11, Apple Silicon);
# sampled output is only bit-for-bit reproducible with these exact versions.
anticipation @ git+https://github.com/jthickstun/anticipation.git@af37397922665a0fb8d474d7988b0f3755a38d45
torch==2.10.0
transformers==5.3.0
safetensors==0.6.2
huggingface_hub==1.7.1
tokenizers==0.22.2
mido==1.3.3
numpy==1.26.2
tqdm==4.67.3
